# @shumaf/vue-shum-ui

Библиотека UI компонентов для Vue 3 + TypeScript на основе **Ant Design Vue**. Проект предназначен для демонстрации компонентов и использования в других проектах.

## 🎯 Цель проекта

Проект создан с целью:

1. **Демонстрация компонентов** - показать возможности библиотеки через интерактивное демо-приложение
2. **Переиспользование в других проектах** - использовать эту библиотеку как основу для разработки новых приложений
3. **Единообразный дизайн** - обеспечить консистентный UI во всех проектах через единую дизайн-систему

## 🎨 Дизайн-система и стили

### Ant Design Vue

Библиотека использует **Ant Design Vue 4.x** в качестве дизайн-системы. Все компоненты являются обертками над нативными компонентами Ant Design, что обеспечивает:

- Единообразный стиль всех компонентов
- Богатый функционал из коробки
- Активную поддержку и обновления
- Гибкую систему кастомизации

### Формат стилей

- **CSS** - стили Ant Design импортируются через `ant-design-vue/dist/reset.css`
- **Scoped CSS** - компоненты используют scoped стили только при необходимости
- **CSS Variables** - поддержка кастомизации через CSS переменные Ant Design

Подробнее: [Документация по Ant Design](./docs_vue-ui-kit/ANT_DESIGN_SETUP.md)

## 🛠 Технологии

- **Vue 3.4+** - Composition API, `<script setup>`
- **TypeScript** - строгая типизация
- **Ant Design Vue 4.x** - дизайн-система
- **Vite** - сборщик и dev-сервер
- **Vitest** - тестирование
- **ESLint + Prettier** - линтинг и форматирование

## 📦 Установка

```bash
npm install @shumaf/vue-shum-ui ant-design-vue
```

## 🚀 Использование

### Базовое подключение

```typescript
import { createApp } from 'vue'
import App from './App.vue'
import Antd from 'ant-design-vue'
import 'ant-design-vue/dist/reset.css'
import VueShumUi from '@shumaf/vue-shum-ui'

const app = createApp(App)
app.use(Antd)
app.use(VueShumUi)
app.mount('#app')
```

> **Важно:** Ant Design Vue должен быть установлен и подключен перед использованием библиотеки.

### Импорт отдельных компонентов

```vue
<script setup lang="ts">
import { Button, Input, Modal } from '@shumaf/vue-shum-ui'
</script>

<template>
  <Button variant="primary" size="medium">Click me</Button>
</template>
```

## 📚 Компоненты

### Button

Кнопка с различными вариантами и размерами (обертка над `a-button`).

```vue
<Button variant="primary" size="medium" :loading="false">
  Click me
</Button>
```

**Варианты:** `primary`, `secondary`, `danger`, `ghost`  
**Размеры:** `small`, `medium`, `large`

### Input

Поле ввода с поддержкой валидации (обертка над `a-input`).

```vue
<Input
  v-model="value"
  type="text"
  placeholder="Enter text"
  :error="false"
/>
```

**Типы:** `text`, `email`, `password`, `number`, `tel`  
**Состояния:** `error`, `success`

### Modal

Модальное окно с различными размерами (обертка над `a-modal`).

```vue
<Modal v-model="isOpen" size="medium" title="Modal Title">
  <template #header>
    <h3>Custom Header</h3>
  </template>
  <p>Modal content</p>
  <template #footer>
    <Button @click="isOpen = false">Close</Button>
  </template>
</Modal>
```

**Размеры:** `small` (400px), `medium` (600px), `large` (800px), `fullscreen` (95vw)

## 🏗 Структура проекта

```
vue-ui-kit/
├── src/
│   ├── components/          # Компоненты библиотеки
│   │   ├── Button/
│   │   ├── Input/
│   │   └── Modal/
│   ├── demo/                # Демо-приложение
│   │   ├── App.vue
│   │   ├── main.ts
│   │   └── pages/
│   ├── types/               # TypeScript типы
│   ├── index.ts             # Точка входа библиотеки
│   └── style.css            # Базовые стили
├── docs_vue-ui-kit/         # Документация
├── .github/workflows/       # GitHub Actions
└── package.json
```

## 💻 Разработка

```bash
# Установка зависимостей
npm install

# Запуск dev сервера (демо приложение)
npm run dev

# Сборка библиотеки
npm run build:lib

# Сборка демо приложения
npm run build:demo

# Тесты
npm run test

# Линтинг
npm run lint

# Форматирование
npm run format
```

## 🎨 Демо-приложение

Демо-приложение демонстрирует все компоненты библиотеки с примерами использования:

```bash
npm run dev
```

Доступно по адресу: `http://localhost:5173`

**Онлайн версия:** [GitHub Pages](https://shumafbatov.github.io/vue-shum-ui/)

## 📦 Публикация

### Управление версиями

Версии управляются автоматически через GitHub Actions:

- **patch** - для исправлений багов (1.0.0 → 1.0.1)
- **minor** - для новых функций (1.0.0 → 1.1.0)
- **major** - для breaking changes (1.0.0 → 2.0.0)

Версия обновляется автоматически при коммитах с префиксами:
- `fix:` или `bugfix:` → patch
- `feat:` или `feature:` → minor
- `BREAKING:` → major

Или вручную через GitHub Actions workflow_dispatch.

### Публикация в npm

После обновления версии пакет автоматически публикуется в npm: `@shumaf/vue-shum-ui`

### Деплой демо

Демо-приложение автоматически деплоится на GitHub Pages при каждом push в `main`/`master`.

## 🔮 Будущее развитие

Планируется использование этой библиотеки в качестве основы для:

- Новых веб-приложений
- Портфолио проектов
- Административных панелей
- Других проектов, требующих единообразного UI

Библиотека будет расширяться новыми компонентами по мере необходимости.

## 📖 Документация

- [Настройка Ant Design](./docs_vue-ui-kit/ANT_DESIGN_SETUP.md)
- [Публикация в npm](./docs_vue-ui-kit/NPM_PUBLISH_403_FIX.md)
- [GitHub Actions](./docs_vue-ui-kit/GITHUB_ACTIONS_GUIDE.md)

## 📄 Лицензия

MIT
